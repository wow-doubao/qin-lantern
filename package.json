{
  "packageManager": "pnpm@10.14.0",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "ui:build": "pnpm --filter qin-lantern build",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build",
    "utils:build": "pnpm --filter utils build",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.2.1",
    "eslint": "^9.33.0",
    "typescript": "^5.9.2"
  }
}
